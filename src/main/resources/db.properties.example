# Copy this file to db.properties (same folder) and adjust for your local PostgreSQL login.
# db.properties is gitignored — every developer edits their own copy.

db.url=jdbc:postgresql://localhost:5432/speakout
db.user=your_postgres_username
db.password=
